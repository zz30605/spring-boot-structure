package spring.boot.structure.config.repository;

//import net.funpodium.services.traffic.entity.Rule;
import org.springframework.data.repository.CrudRepository;

import java.util.List;
import java.util.Optional;

//public interface RuleRepository extends CrudRepository<Rule, Long> {
    //find if data is stored in database
//    Optional<Rule> findByCountryAndCompanyAndBrandAndPlatformAndLocale(String country, String company, String brand, String platform, String locale);
//
//    Optional<Rule> findById(Long id);
//
//    Optional<List<Rule>> findByCompanyAndBrand(String company, String brand);

//}
